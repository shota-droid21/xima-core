user xima xima;

# When running nginx as non-root (uid=1000), the default pid path under /run may be unwritable.
pid /tmp/nginx.pid;

worker_processes auto;

events {
    worker_connections 4096;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Avoid permission issues when running workers as uid=1000.
    access_log /dev/stdout;
    error_log /dev/stderr warn;

    sendfile on;
    tcp_nopush on;
    keepalive_timeout 65;

    # Reduce file metadata I/O for hot static/thumb reads.
    open_file_cache max=10000 inactive=30s;
    open_file_cache_valid 60s;
    open_file_cache_min_uses 2;
    open_file_cache_errors on;

    # FastAPI upstream
    upstream xima_api {
        server host.docker.internal:27800;
        keepalive 64;
    }

    server {
        listen 8080;

        location /thumbs/ {
            # Handle CORS for OPTIONS requests
            if ($request_method = 'OPTIONS') {
                add_header 'Access-Control-Allow-Origin' '*' always;
                add_header 'Access-Control-Allow-Methods' 'GET, HEAD, OPTIONS' always;
                add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization' always;
                add_header 'Access-Control-Max-Age' 1728000 always;
                add_header 'Content-Type' 'text/plain; charset=utf-8' always;
                add_header 'Content-Length' 0 always;
                return 204;
            }

            # Only allow methods needed for static delivery.
            if ($request_method !~ ^(GET|HEAD|OPTIONS)$) {
                return 405;
            }

            # CORS headers for actual responses
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Allow-Methods' 'GET, HEAD, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization' always;
            add_header 'Access-Control-Expose-Headers' 'Content-Length,Content-Range' always;

            alias /data/workspaces/;
            access_log off;
            add_header Cache-Control "private, max-age=31536000, immutable" always;
            try_files $uri =404;
        }

        # /static/<path-under-workspaces-root> -> /data/workspaces/<path-under-workspaces-root>
        location /static/ {
            # Handle CORS for OPTIONS requests
            if ($request_method = 'OPTIONS') {
                add_header 'Access-Control-Allow-Origin' '*' always;
                add_header 'Access-Control-Allow-Methods' 'GET, HEAD, OPTIONS' always;
                add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization' always;
                add_header 'Access-Control-Max-Age' 1728000 always;
                add_header 'Content-Type' 'text/plain; charset=utf-8' always;
                add_header 'Content-Length' 0 always;
                return 204;
            }

            # Only allow methods needed for static delivery.
            if ($request_method !~ ^(GET|HEAD|OPTIONS)$) {
                return 405;
            }

            # CORS headers for actual responses
            add_header 'Access-Control-Allow-Origin' '*' always;
            add_header 'Access-Control-Allow-Methods' 'GET, HEAD, OPTIONS' always;
            add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization' always;
            add_header 'Access-Control-Expose-Headers' 'Content-Length,Content-Range' always;

            alias /data/workspaces/;
            access_log off;
            add_header Cache-Control "private, max-age=31536000, immutable" always;
            try_files $uri =404;
        }

        # /health endpoint - no auth required
        location = /health {
            proxy_pass http://xima_api;
            proxy_http_version 1.1;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # All other API requests - open mode (no auth_request)
        location / {
            proxy_pass http://xima_api;
            proxy_http_version 1.1;

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Dummy identity headers for open mode (for downstream compatibility)
            proxy_set_header X-Auth-Mode "open";
            proxy_set_header X-User-Id "open";
            proxy_set_header X-User-Name "open";
            proxy_set_header X-User-Email "open@example.com";

            client_max_body_size 100m;
        }
    }
}
